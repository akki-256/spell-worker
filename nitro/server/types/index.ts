export type SpellMessage = {
  userSpell: string;
  correctSpell: string;
};

export type SensorData = {
  accel_x: number;
  accel_y: number;
  accel_z: number;
  angle_x: number;
  angle_y: number;
};
